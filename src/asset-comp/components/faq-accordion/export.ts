import type { ExportResult } from "@/lib/registry/types";
import type { Node } from "@/lib/schema/types";
import { escapeHtml, propString } from "@/lib/registry/shared";
import { listValue, nodeList, uniqueId } from "../_shared/content";
interface Faq{id:string;question:string;answer:string;}
const FALLBACK:Faq[]=[{id:"duration",question:"Berapa lama proses pembuatan website?",answer:"Ruang lingkup sederhana biasanya dapat selesai dalam 2–4 minggu. Kami menyepakati jadwal yang jelas sebelum pekerjaan dimulai."},{id:"edit",question:"Apakah saya bisa mengubah isi setelah website selesai?",answer:"Bisa. Anda dapat mengubah teks, gambar, tautan, warna, dan susunan bagian langsung dari builder tanpa menulis kode."},{id:"mobile",question:"Apakah website ini nyaman dibuka dari ponsel?",answer:"Ya. Setiap bagian sudah dirancang responsif, lalu dapat diperiksa kembali melalui mode desktop, tablet, dan ponsel di kanvas."},{id:"grow",question:"Bagaimana jika kebutuhan saya bertambah?",answer:"Mulai dari struktur yang paling penting terlebih dahulu. Bagian baru dapat ditambahkan kapan saja saat bisnis Anda tumbuh."}];
function items(node:Node):Faq[]{const list=nodeList(node,"items").map((item,index)=>{const question=listValue(item,"question");const answer=listValue(item,"answer");if(!question||!answer)return null;return{id:uniqueId("faq",index,question),question,answer};}).filter((item):item is Faq=>item!==null);return list.length?list:FALLBACK;}
export function faqAccordionExport(node:Node):ExportResult{const eyebrow=propString(node,"eyebrow").trim()||"Pertanyaan yang sering muncul";const title=propString(node,"title").trim()||"Semua yang perlu Anda tahu sebelum mulai.";const description=propString(node,"description").trim()||"Jika jawabannya belum ada di sini, Anda selalu bisa menghubungi kami untuk berdiskusi.";const entries=items(node).map((item,index)=>`<details class="bi-faq-item"${index===0?" open":""}><summary>${escapeHtml(item.question)}<span aria-hidden="true">⌄</span></summary><p>${escapeHtml(item.answer)}</p></details>`).join("");return{html:`<section class="bi-faq"><div class="bi-container bi-faq-layout"><header><p>${escapeHtml(eyebrow)}</p><h2 class="bi-title">${escapeHtml(title)}</h2><span>${escapeHtml(description)}</span></header><div class="bi-faq-list">${entries}</div></div></section>`,css:`.bi-faq{padding-block:clamp(3.5rem,8vw,6.5rem);background:color-mix(in srgb,var(--bi-secondary) 30%,var(--bi-bg));color:var(--bi-fg)}.bi-faq-layout{display:grid;grid-template-columns:.8fr 1.2fr;gap:4rem}.bi-faq header>p{margin:0;color:var(--bi-primary);font-size:.68rem;font-weight:800;letter-spacing:.14em;text-transform:uppercase}.bi-faq h2{margin:.75rem 0 0}.bi-faq header>span{display:block;margin-top:1rem;color:var(--bi-muted-fg);font-size:.95rem;line-height:1.7}.bi-faq-list{padding:0 1.25rem;border:1px solid var(--bi-border);border-radius:var(--bi-radius);background:var(--bi-card)}.bi-faq-item{padding:.2rem 0;border-top:1px solid var(--bi-border)}.bi-faq-item:first-child{border-top:0}.bi-faq-item summary{display:flex;min-height:3.75rem;align-items:center;justify-content:space-between;gap:1rem;cursor:pointer;font-size:.9rem;font-weight:700;list-style:none}.bi-faq-item summary::-webkit-details-marker{display:none}.bi-faq-item summary span{display:grid;width:1.75rem;height:1.75rem;flex:none;place-items:center;border-radius:50%;background:var(--bi-secondary);color:var(--bi-primary);font-size:1rem;transition:transform .2s ease}.bi-faq-item[open] summary span{transform:rotate(180deg)}.bi-faq-item p{max-width:38rem;margin:0;padding:0 0 1.25rem;color:var(--bi-muted-fg);font-size:.9rem;line-height:1.7}@media(max-width:760px){.bi-faq-layout{grid-template-columns:1fr;gap:2rem}}`};}
