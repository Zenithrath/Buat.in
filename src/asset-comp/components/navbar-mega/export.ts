import type { ExportResult } from "@/lib/registry/types";
import type { Node } from "@/lib/schema/types";
import { escapeHtml, propString, sanitizeUrl } from "@/lib/registry/shared";
import { listValue, nodeList, uniqueId } from "../_shared/content";
import { navInstanceId } from "../_shared/navigation";

interface MegaColumn { id: string; heading: string; links: { id: string; label: string; url: string }[]; }
const FALLBACK: MegaColumn[] = [
  { id: "bisnis", heading: "Untuk bisnis", links: [{ id: "profil", label: "Profil perusahaan", url: "#profil" }, { id: "toko", label: "Toko online", url: "#toko" }, { id: "karya", label: "Portofolio", url: "#karya" }] },
  { id: "solusi", heading: "Solusi", links: [{ id: "mulai", label: "Mulai dari nol", url: "#mulai" }, { id: "desain", label: "Rancang ulang", url: "#desain" }, { id: "konsultasi", label: "Konsultasi", url: "#konsultasi" }] },
  { id: "sumber", heading: "Sumber daya", links: [{ id: "panduan", label: "Panduan singkat", url: "#panduan" }, { id: "cerita", label: "Kisah pelanggan", url: "#cerita" }, { id: "kontak", label: "Hubungi tim", url: "#kontak" }] },
];
function columns(node: Node): MegaColumn[] { const items = nodeList(node, "columns").map((item, index) => { const heading = listValue(item, "heading").trim(); if (!heading) return null; const links = [1, 2, 3].map((number) => { const label = listValue(item, `link${number}Label`).trim(); return label ? { id: uniqueId("mega-link", number, label), label, url: sanitizeUrl(listValue(item, `link${number}Url`, "#")) } : null; }).filter((link): link is { id: string; label: string; url: string } => link !== null); return { id: uniqueId("mega-column", index, heading), heading, links }; }).filter((column): column is MegaColumn => column !== null); return items.length ? items : FALLBACK; }

export function navbarMegaExport(node: Node): ExportResult {
  const logo = propString(node, "logoText").trim() || "SORA";
  const menuText = propString(node, "menuText").trim() || "Jelajahi";
  const sideText = propString(node, "linkText").trim() || "Tentang kami";
  const sideUrl = sanitizeUrl(propString(node, "linkUrl").trim() || "#tentang");
  const cta = propString(node, "ctaText").trim() || "Jadwalkan demo";
  const ctaUrl = sanitizeUrl(propString(node, "ctaUrl").trim() || "#kontak");
  const instance = navInstanceId(node, "mega-nav"); const panelId = `${instance}-panel`;
  const columnHtml = columns(node).map((column) => `<section class="bi-mega-column"><p>${escapeHtml(column.heading)}</p>${column.links.map((link) => `<a href="${escapeHtml(link.url)}">${escapeHtml(link.label)} <span aria-hidden="true">→</span></a>`).join("")}</section>`).join("");
  return { html: `<header class="bi-mega-wrap"><nav class="bi-mega-nav" data-bi-mega-nav="${instance}" data-open="false" aria-label="Navigasi utama"><a class="bi-mega-brand" href="#"><i aria-hidden="true"></i>${escapeHtml(logo)}</a><div class="bi-mega-desktop"><button type="button" data-bi-mega-toggle aria-expanded="false" aria-controls="${panelId}">${escapeHtml(menuText)} <span aria-hidden="true">⌄</span></button><a href="${escapeHtml(sideUrl)}">${escapeHtml(sideText)}</a><a class="bi-mega-cta" href="${escapeHtml(ctaUrl)}">${escapeHtml(cta)}</a></div><button class="bi-mega-mobile-toggle" data-bi-mega-mobile-toggle type="button" aria-label="Buka menu" aria-expanded="false" aria-controls="${panelId}"><i></i><i></i></button><div class="bi-mega-panel" id="${panelId}">${columnHtml}<aside><b aria-hidden="true">✦</b><strong>Mari rancang sesuatu yang terasa tepat.</strong><a href="${escapeHtml(ctaUrl)}">${escapeHtml(cta)} <span aria-hidden="true">→</span></a></aside></div></nav></header>`, css: `.bi-mega-wrap{position:relative;z-index:30;padding:1rem 1.5rem;background:var(--bi-bg);color:var(--bi-fg);font-family:var(--bi-font-body)}.bi-mega-nav{position:relative;display:flex;max-width:72rem;align-items:center;justify-content:space-between;gap:1rem;margin:auto}.bi-mega-brand{display:flex;align-items:center;gap:.5rem;color:var(--bi-fg);font-family:var(--bi-font-heading);font-size:1.05rem;font-weight:900;letter-spacing:.06em;text-decoration:none}.bi-mega-brand i{width:.5rem;height:.5rem;border-radius:50%;background:var(--bi-primary)}.bi-mega-desktop{display:flex;align-items:center;gap:1.5rem}.bi-mega-desktop>a,.bi-mega-desktop button{border:0;background:transparent;color:var(--bi-muted-fg);font:650 .88rem/1 var(--bi-font-body);text-decoration:none;cursor:pointer;transition:color .2s ease}.bi-mega-desktop>a:hover,.bi-mega-desktop button:hover{color:var(--bi-fg)}.bi-mega-desktop button span{display:inline-block;transition:transform .2s ease}.bi-mega-nav[data-open="true"] .bi-mega-desktop button span{transform:rotate(180deg)}.bi-mega-cta{padding:.72rem 1rem;border-radius:999px;background:var(--bi-primary)!important;color:var(--bi-primary-fg)!important;font-size:.75rem!important;font-weight:800!important}.bi-mega-panel{position:absolute;top:calc(100% + 1rem);right:0;left:0;display:grid;grid-template-columns:repeat(3,minmax(0,1fr)) minmax(12rem,.9fr);overflow:hidden;border:1px solid var(--bi-border);border-radius:calc(var(--bi-radius) * 1.1);background:var(--bi-card);box-shadow:var(--bi-shadow);opacity:0;pointer-events:none;transform:translateY(-.5rem);visibility:hidden;transition:opacity .2s ease,transform .2s ease,visibility .2s ease}.bi-mega-nav[data-open="true"] .bi-mega-panel{opacity:1;pointer-events:auto;transform:translateY(0);visibility:visible}.bi-mega-column{min-width:0;padding:1.25rem;border-right:1px solid var(--bi-border)}.bi-mega-column p{margin:0 0 .7rem;color:var(--bi-primary);font-size:.65rem;font-weight:900;letter-spacing:.14em;text-transform:uppercase}.bi-mega-column a{display:flex;align-items:center;justify-content:space-between;gap:.5rem;padding:.6rem;border-radius:calc(var(--bi-radius) * .65);color:var(--bi-fg);font-size:.87rem;font-weight:650;text-decoration:none;transition:background .18s ease}.bi-mega-column a:hover{background:var(--bi-muted)}.bi-mega-column a span{color:var(--bi-muted-fg);transition:transform .18s ease}.bi-mega-column a:hover span{color:var(--bi-primary);transform:translateX(2px)}.bi-mega-panel aside{display:flex;min-height:12rem;flex-direction:column;justify-content:flex-end;padding:1.25rem;background:var(--bi-primary);color:var(--bi-primary-fg)}.bi-mega-panel aside b{margin-bottom:auto;font-size:1.1rem}.bi-mega-panel aside strong{font:800 1.2rem/1.15 var(--bi-font-heading);letter-spacing:-.04em}.bi-mega-panel aside a{margin-top:1rem;color:inherit;font-size:.75rem;font-weight:800;text-decoration:underline;text-underline-offset:4px}.bi-mega-mobile-toggle{display:none}@media(max-width:720px){.bi-mega-wrap{padding:.85rem 1rem}.bi-mega-desktop{display:none}.bi-mega-mobile-toggle{display:grid;width:2.25rem;height:2.25rem;place-content:center;gap:.28rem;border:1px solid var(--bi-border);border-radius:50%;background:var(--bi-card);color:var(--bi-fg);cursor:pointer}.bi-mega-mobile-toggle i{display:block;width:.95rem;height:1.5px;background:currentColor;transition:transform .2s ease}.bi-mega-nav[data-open="true"] .bi-mega-mobile-toggle i:first-child{transform:translateY(2px) rotate(45deg)}.bi-mega-nav[data-open="true"] .bi-mega-mobile-toggle i:last-child{transform:translateY(-2px) rotate(-45deg)}.bi-mega-panel{grid-template-columns:1fr;max-height:min(76vh,38rem);overflow:auto}.bi-mega-column{border-right:0;border-bottom:1px solid var(--bi-border)}.bi-mega-panel aside{min-height:9rem}}`, js: `document.querySelectorAll('[data-bi-mega-nav="${instance}"]').forEach(function(nav){var buttons=nav.querySelectorAll('[data-bi-mega-toggle],[data-bi-mega-mobile-toggle]');function close(){nav.dataset.open='false';buttons.forEach(function(button){button.setAttribute('aria-expanded','false');});}buttons.forEach(function(button){button.addEventListener('click',function(event){event.stopPropagation();var open=nav.dataset.open==='true';nav.dataset.open=String(!open);buttons.forEach(function(item){item.setAttribute('aria-expanded',String(!open));});});});document.addEventListener('click',function(event){if(!nav.contains(event.target))close();});document.addEventListener('keydown',function(event){if(event.key==='Escape')close();});nav.querySelectorAll('.bi-mega-panel a').forEach(function(link){link.addEventListener('click',close);});});` };
}
